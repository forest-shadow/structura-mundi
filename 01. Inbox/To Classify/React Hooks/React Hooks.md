---
aliases:
  - Хуки React
note_type: overview
area: computer-science
domain: frontend-engineering
section: react-hooks
parent:
status: seed
related:
  - "[[Rules of Hooks]]"
  - "[[useState]]"
  - "[[useEffect]]"
tags: []
---

# React Hooks

## Краткое определение области

React Hooks - это механизм React, который позволяет использовать состояние, эффекты, контекст и другие возможности React внутри функциональных компонентов и custom hooks.

## Что входит в эту тему

- правила использования hooks;
- базовые встроенные hooks;
- паттерн создания custom hooks;
- границы между локальным состоянием, побочными эффектами и повторным использованием логики.

## Как устроена ветка

- На первом уровне достаточно одной обзорной заметки.
- Вложенный `overview` пока не нужен.
- Дочерние темы лучше держать как плоские `article`.

## Рекомендуемый маршрут чтения

1. Начать с этой заметки.
2. Затем перейти к `Rules of Hooks`.
3. После этого прочитать `useState`, `useEffect`, `useReducer`, `useContext`, `useRef` и `Custom Hooks`.

## Соседние overview-ветки

Позже эта тема может быть связана с более широкой веткой по `React`, но пока отдельный обзорный слой не обязателен.

## Что стоит раскрыть дальше

- [ ] Уточнить границы темы.
- [ ] Проверить, какие hooks действительно нужны как отдельные статьи.
- [ ] Проверить `related`.
- [ ] Не создавать лишний промежуточный уровень.
